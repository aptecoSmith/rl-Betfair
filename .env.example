# Copy this file to .env and fill in real values.
# Never commit .env to source control.

# MySQL credentials (Docker, localhost:3307)
DB_USER=root
DB_PASSWORD=your_password_here

# Optional: override DB host/port if not using Docker defaults
# DB_HOST=localhost
# DB_PORT=3307
