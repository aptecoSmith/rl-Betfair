# URLs to ingest (shared)

Add one URL per line below. Run `python scripts/wiki_tool.py scan` to ingest them.
Status is annotated inline after each run: `[done]`, `[failed: reason]`.

<!-- example:
- https://example.com/some-article
-->
