# Hard Constraints

- No backend changes — purely frontend.
- Help text must be factually accurate. If a field's behaviour is
  unclear, read the backend code before writing the help text.
- Visual style must be consistent with the wizard page's existing
  `.help-text` and `.field-help` classes.
- Help text must not break the form layout on narrow screens.
- Frontend builds clean: `ng build`.
