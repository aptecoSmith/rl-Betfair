session: smoke
seed: 0
dataset:
  feature_variant: V1
  train_corpus: tvl_mask_29d
  horizons: [3m, 7m]
architecture:
  family: mlp
  variant_label: d2
  kwargs:
    depth: 2
    hidden: 64
    dropout: 0.1
output:
  formulation: pinball3
  quantiles: [0.1, 0.5, 0.9]
training:
  batch_size: 2048
  learning_rate: 0.001
  max_epochs: 6
  early_stopping_patience: 2
device: cuda
