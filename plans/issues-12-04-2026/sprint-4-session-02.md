# Sprint 4, Session 2: Manual Evaluation — Frontend (Issue 04, Session 2)

Read `CLAUDE.md` and `plans/issues-12-04-2026/04-manual-evaluation/`
before starting. Follow session 2 of `master_todo.md`.

**Prerequisite:** Sprint 4 session 1 (backend) must be done.

## Scope

Build the evaluation UI:

1. Evaluation page: model picker, date picker, evaluate button,
   progress display, results summary
2. "Re-evaluate" button on model detail page with date picker dialog
3. Multi-select checkboxes on scoreboard + "Evaluate selected" bulk action
4. Optionally extract shared progress bar component

See session 2 of `master_todo.md` for the full task list.

## Commit

`feat: evaluation UI — model picker, date picker, re-evaluate button, bulk select`
Push: `git push all`
